# Projects

| Project | Repository |
|---------|------------|
| Cast defect CNN | [cnn-cast-defect-classification](https://github.com/mentisVeritas/cnn-cast-defect-classification) |
| Chair detection (YOLO) | in this repo: `chair_detection_yolo/` |

After clone, unpack YOLO data:

```bash
cd chair_detection_yolo
unzip -q -o data/dataset.zip -d data
```
